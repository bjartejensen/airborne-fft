import { FftChartDirective } from './fft-chart.directive';

describe('FftChartDirective', () => {
  it('should create an instance', () => {
    const directive = new FftChartDirective();
    expect(directive).toBeTruthy();
  });
});
