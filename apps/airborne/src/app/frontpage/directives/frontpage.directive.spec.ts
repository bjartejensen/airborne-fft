import { FrontpageDirective } from './frontpage.directive';

describe('FrontpageDirective', () => {
  it('should create an instance', () => {
    const directive = new FrontpageDirective();
    expect(directive).toBeTruthy();
  });
});
