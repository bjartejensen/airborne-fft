import { FrontpageChartDirective } from './frontpage-chart.directive';

describe('FrontpageChartDirective', () => {
  it('should create an instance', () => {
    const directive = new FrontpageChartDirective();
    expect(directive).toBeTruthy();
  });
});
